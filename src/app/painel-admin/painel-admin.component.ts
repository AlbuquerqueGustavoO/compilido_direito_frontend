import { Component } from '@angular/core';

// Mocado por enquanto — só a rota, o guard de perfil e o visual do shell.
// Gestão de usuários (listar, trocar perfil, excluir) entra depois.
@Component({
  selector: 'app-painel-admin',
  templateUrl: './painel-admin.component.html',
  styleUrls: ['./painel-admin.component.scss'],
})
export class PainelAdminComponent { }
